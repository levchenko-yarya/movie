<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genre</title>
</head>
<body>
<div class="new" style="width: 50%; float: left">
    <p style="text-align: center">жанры:</p>
    {{#each genres}}
        <div class="card movie" style="width: 28rem;">
            <div class="card-body">
                <h5 class="card-title"><a href="/movie/{{this.id}}">{{ this.title }}</a></h5>
                <p class="card-text">{{ this.description }}</p>
                <p class="card-text">{{ this.genre }}</p>
                <a href="{{ this.url }}" class="btn btn-primary" target="_blank">ссылка на трейлер</a>
            </div>
        </div>
    {{/each}}
</div>
</body>
</html>